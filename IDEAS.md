# Ideas for improving Virtual-Produino

- Dynamic lookup of the arduino somehow. UDP Broadcast? 
    Then the system can be plugged in, and the application can connect
    at any point after. without the user needing to know the IP:Port

- The Arduino sketch only uses like ~30% of the available ROM. We could add 
    in history, or something more. There is space for it.

- Image sampling can be controlled by the user. We could give 
    choice in algorithm, and choice in sample location.

- should double clicking an item in the image selection 
    send that image to the arduino instead of removing it?
    either is valid, yet what is more intuitive?